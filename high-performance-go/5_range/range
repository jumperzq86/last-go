range 在迭代过程中返回的是迭代值的拷贝，因此需要注意两点
1. 修改返回的迭代值，对原数据(slice/map)没有影响，除非返回的迭代值类型为指针
2. 若是迭代的元素的内存占用越高，比如一个包含很多属性的struct结构体，那么for性能将显著高于range。
   对于这种情况，两种处理方式来改善range性能
   1> 只迭代下标，通过下标访问迭代值
   2> 将切片/数组的元素改为指针